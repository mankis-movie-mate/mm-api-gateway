http:
  middlewares:
    auth:
      forwardAuth:
        address: "${USER_SERVICE_URL}/validate"
        trustForwardHeader: true
        authResponseHeaders:
          - "X-User-Id"
          - "X-User-Username"
          - "X-User-Email"
          - "X-User-Roles"
