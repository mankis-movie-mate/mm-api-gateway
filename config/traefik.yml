api:
  dashboard: true
  insecure: false

entryPoints:
  internal:
    address: ":8080"

providers:
  file:
    filename: "/etc/traefik/dynamic.yml"

log:
  level: DEBUG
  format: common

accessLog:
  format: common
